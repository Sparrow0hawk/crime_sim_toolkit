language: python
python:
  - "3.6"

install:
  - pip install -r requirements.txt

script:
  - python setup.py install
  - travis_wait 30 coverage run tests/test_all.py
  - coverage xml
  - python-codacy-coverage -r coverage.xml
